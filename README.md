# Rust Rest API - Actix web

## Before start
```sh
cargo install --locked watchexec-cli
```

## Development
1. Create .env file based on .env.example
2. Run command
```sh
watchexec -e rs -r cargo run   
```